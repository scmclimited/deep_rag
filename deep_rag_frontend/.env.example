# =============================================================================
# Deep RAG Frontend - Environment Configuration
# =============================================================================
# This file contains environment variables specific to the Frontend service.
# The frontend is a Streamlit application that provides a web UI for document
# ingestion and querying.
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# =============================================================================
# API CONNECTION CONFIGURATION
# =============================================================================
# Backend API URL for the frontend to connect to.
# The frontend uses this URL to communicate with the backend API service.

# When running in Docker Compose (full stack): http://api:8000
# When running frontend standalone with local backend: http://host.docker.internal:8000
# When running frontend standalone with remote backend: http://your-backend-url:8000
API_BASE_URL=http://localhost:8000

# =============================================================================
# STREAMLIT CONFIGURATION
# =============================================================================
# Streamlit server settings (optional - defaults are usually sufficient).

# Frontend port (default: 8501)
FRONTEND_PORT=8501

# Streamlit server configuration (set automatically by Docker, but can be overridden)
# STREAMLIT_SERVER_PORT=8501
# STREAMLIT_SERVER_ADDRESS=0.0.0.0
# STREAMLIT_SERVER_HEADLESS=true
# STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
