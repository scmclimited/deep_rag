# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# 
# This .env file is for standalone database service use.
# When running the database independently from vector_db/ directory,
# create a .env file from this template.
#
# For full stack use, use the root .env file (deep_rag/.env.example)
# =============================================================================

# PostgreSQL with pgvector
# Database host (use 'localhost' for local connections, 'db' when connecting from other Docker containers)
DB_HOST=localhost

# Database port
DB_PORT=5432

# Database user
DB_USER=user_here

# Database password
DB_PASS=password_here

# Database name
DB_NAME=ragdb
